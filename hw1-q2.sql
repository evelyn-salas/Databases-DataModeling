--2. (8 points) In this question, we will be creating a table with attributes of types integer, varchar, date, and Boolean. 

--Write a Create Table statement and run it to create a table called `MyRestaurants` with the following attributes: 
-- Name of the restaurant: a varchar field called name
-- Type of food they make: a varchar field called cuisine
-- Distance (in minutes) from your house: an int called distance
-- Date of your last visit: a date called lastvisit
-- Whether you like it or not: a bit field called liked

--CREATE TABLE MyRestaurants (
--    name VARCHAR(50),
--    cuisine VARCHAR(50),
--    distance INT,
--    lastvisit date,
--    liked BIT,
--);